# Next Action Research Results

This folder contains external Deep Research outputs and KiSurf's consolidated
research summaries.

Use `next-action-runtime-consolidated-report.md` as the current implementation-oriented
summary unless a newer ADR or implementation spec supersedes it.

The key ideas from `LLM-Mediated Inner Loop for an AI-Native EDA Next Action Runtime.md`
have been folded into the consolidated report: bounded hidden attempts, validator
tools plus mandatory preview/accept gates, runtime-owned governance, provider
tool-call execution with structured tool results, and promotion-only publication.

Use `next-action-source-spike-20260622.md` as the current source-level
implementation spike for routing, placement, preview, validation, accept, and
structured-surface entry points.
